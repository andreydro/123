Что такое переменная?   - именнуемая область памяти, адрес которой можно использовать для осуществления доступа к данным

Как объявить переменную? Как инициализировать переменную? Примеры кода   - var name;

Виды циклов ( 3 вида )? Примеры использования   - while (условие){тело}; do {тело} while (условие); for(начало, условие, шаг){тело};

Сколько и какие типы данных в javascript? Примеры создания переменных каждого типа   -7 типов: string(""), number(1), object(null,{}), undefined, boolean(true), null, symbol

Как узнать тип переменной? Пример кода  - typeof "line" // "string"

Что такое область видимости?   - то откуда видна переменная, вне вункции(глобальная), внутри функции(локальная) 

Как из javascript в брaузере вывести на экран строчку? ( 4-ре способа ) Примеры кода   - alert, return, document.write(x);,print, console.log()

Каким образом можно реализовать условное выполнение кода?  - через использование условных оперторов и тенарного оператора

Условные операторы в javascript - какие? Примеры использования   - if, else, else if, 

Что такое тернарный оператор?   - условный оператор, который принимает три аргумента

Как обойтись без условных операторов если нужно сделать условное выполнение кода? С примером   - искользавать цикл while со словом break

Что такое функция? Какими характеристиками можно описать функцию?    - это обьект, которым можно по разному манипулировать. Например: передеавать как аргумент и возвращать в качестве результата др. функций, создвать анонимно и присваивать в качестве значений переменных или свойств объектов.

Как можно создать функцию? 3 способа. C примерами кода   - function someFun(param) { инструкция }, var someFun = function(param) { инструкция }, var someFun = new function(params, ' инструкции'). 


Что такое объект? Как создать объект? ( 3 способа ) С примерами кода   - набор свойств, и каждое свойство состоит из имени и значения, ассоциированного с этим именем. var animal = new Animal(""); var animal = {}; var animal = Object.create(Animal);

Что такое передача по ссылке / передача по значению?   - По ссылке - При копировании переменной с объектом – копируется эта ссылка, а объект по-прежнему остается в единственном экземпляре;
 По значению - Обычные значения: строки, числа, булевы значения, null/undefined при присваивании переменных копируются целиком или, как говорят, «по значению».

Что такое минификация и зачем она нужна?    - это простой подход для уменьшения размеров файлов css, js и html.

Как можно добавить javascript код в html страницу? 2 способа. C примерами кода     - <script>код</script>, <script src="main.js"></script>

Что такое массив?   - разновидность объекта, которая предназначена для хранения пронумерованных значений и предлагает дополнительные методы для удобного манипулирования такой коллекцией.

Как создать массив? ( 3 способа ) С примером кода   -  var arr = [];,  var arr = new Array();,  или тоже самое со значениями

Как запросить данные у пользователя? с примером кода   - result = prompt(title, default);

Что такое замыкание?   -  это особый вид функции. Она определена в теле другой функции и создаётся каждый раз во время её выполнения. Синтаксически это выглядит как функция, находящаяся целиком в теле другой функции. При этом вложенная внутренняя функция содержит ссылки на локальные переменные внешней функции.

Как узнать сколько параметров принимает функция?    - посмотреть в скобки после нее

Как узнать есть ли у объекта свойство x ?    -  x in {x:13}, hasOwnProperty


Как сделать ревес строки?    - string.split("").reverse().join("");

Как удалить предпоследний элемент в массиве?     - delete arr[arr.length-1]

Как заменить в строке "papa roach" все буквы a на A ?  - ???????????

Сколькими способами ( с примерами кода ) можно найти на странице div с id someId ?   -  var element = getElementById(id);          
                                                                                        myArray.find(x => x.id === '45').foo;

Как устроен событийный цикл в js?   -  Каждое событие должно быть полностью обработано прежде, чем начнет обрабатываться следующее. Благодаря этому мы можем точно знать: когда бы и где бы не выполнялась функция – она не может быть приостановлена и будет целиком завершена до начала выполнения другого кода

Что такое API? Примеры? это готовый код для упрощения жизни программисту (jQuery, )

Как в js реализовать многопоточность?    -  никак.

Как в js реализовать наследование одного "класса" от другого? Пример кода    - Rabbit.prototype.__proto__ = Animal.prototype;

Сколькими способами можно проитерироваться по всем полям объекта? С примерами кода  
var i = 0;
while (i < 3) {
  alert( i );
  i++;
}

Что такое callback ? Зачем? Пример кода    -  это функция, которая должна быть выполнена после того, как другая функция завершила выполнение (отсюда и название: callback – функция обратного вызова)

Почему 1 + '2' ? и 1 - '2' ? = 12 и -1  + работает еще и со стройкой, а - только с числом

Как и когда работает преобразование типов? 
- Строковое преобразование происходит, когда требуется представление чего-либо в виде строки   alert(a);
- Численное преобразование происходит в математических функциях и выражениях, а также при сравнении данных различных типов (кроме сравнений ===, !==)  var a = +"123";                           var a = Number("123");
- Boolean преобразование к true/false происходит в логическом контексте, таком как if(value), и при применении логических операторов.

Что такое HTTP ? Из чего состоит HTTP протокол?   
-   протокол прикладного уровня передачи данных
Три категории:    Серверы как основные поставщики услуг хранения и обработки информации (обработка запросов);
    			  Клиенты — конечные потребители услуг сервера (отправка запроса);
                  Прокси (посредники) для выполнения транспортных служб.


Какие есть методы HTTP запросов? Для чего каждый?
- GET -  для запроса содержимого указанного ресурса
- POST -  для передачи пользовательских данных заданному ресурсу.
- HEAD - Аналогичен методу GET, за исключением того, что в ответе сервера отсутствует тело.
- PUT - Применяется для загрузки содержимого запроса на указанный в запросе URI.
- PATCH - Аналогично PUT, но применяется только к фрагменту ресурса.
- DELETE - Удаляет указанный ресурс.
- TRACE - Возвращает полученный запрос так, что клиент может увидеть, какую информацию промежуточные серверы добавляют или изменяют в запросе.
- CONNECT Преобразует соединение запроса в прозрачный TCP/IP-туннель, обычно чтобы содействовать установлению защищённого SSL-соединения через нешифрованный прокси.
- OPTIONS - Используется для определения возможностей веб-сервера или параметров соединения для конкретного ресурса. 


Что такое REST ? RPC ?       REST ( Representational State Transfer)  -  это стиль архитектуры программного обеспечения для распределенных систем, таких как World Wide Web, который, как правило, используется для построения веб-служб
                              RPC (Remote Procedure Call, удаленный вызов процедур) - класс технологий, позволяющих компьютерным программам вызывать функции или процедуры в другом адресном пространстве (как правило, на удалённых компьютерах)


Что такое "линтер" (linter)? Зачем они нужны? Какие линтеры есть для javascript?    - Программа для форматирования кода в соответствии с стандартами. JSLint, JSHint, Closure Linter

Включить в своем редакторе поддержку eslint (установить плагин), чтобы код проверялся по правилам репозитория   - сделал

Настроить автоматическое форматирование, чтобы код форматировался под правила репозитория   - сделал

Что такое "jsdoc" ? Найдите его в своем домашнем задании. Зачем он нужен? Объясните использовнные теги.    -   генератор документации в HTML-формате из комментариев исходного кода на JavaScript.

Что такое пакетный менеджер? Какие пакетные менеджеры есть для js?    - программа для управления процессом установки пакетов. Например: npm, bower, volo, ringojs, component

Что делают системы сборки? Какие системы сборки есть для js?  Собирает проект воедино.Например: Gulp, Grunt, Gear.js, Brunch

Как с помощью gulp минифицировать js файл?
	var gulp = require('gulp'),
	    uglify = require('gulp-uglify');

	gulp.task('minify', function () {
	    gulp.src('js/app.js')
	        .pipe(uglify())
	        .pipe(gulp.dest('build'));
	});

	npm install --save-dev gulp-uglify
